# Egreso — Plan de implementación (Fase Dominio)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir el dominio del egreso (entidades `CategoriaEgreso` y `Egreso` con el guard `Egreso.Registrar`), con TDD, sin tocar la base de datos.

**Architecture:** Módulo nuevo `Finanzas/` en la capa Domain. `Egreso.Registrar` es una fábrica estática que valida los invariantes (monto positivo, categoría asignada) y construye el egreso; guarda el usuario responsable en `UsuarioId`. El dominio es C# puro (sin EF): sus pruebas corren sin base de datos.

**Tech Stack:** .NET 10 · C# · xUnit · capa `MiniERP.Domain` (sin dependencias).

## Global Constraints

- **El dominio no conoce EF Core.** Estas clases y sus pruebas no tocan la base ni el `MiniErpDbContext`.
- **El egreso NO toca inventario ni balances.** Es solo un registro de gasto; por eso un pago a proveedor nunca es un egreso.
- **`UsuarioId` guarda al responsable** del gasto (como `Pago.UsuarioId` y `MovimientoInventario.UsuarioId`), distinto del `CreadoPor` de auditoría. `Registrar` fija ambos.
- **Un solo `Monto`, sin ITBIS. Pagado al registrar** (la `Fecha` es cuándo ocurrió y se pagó).
- **Nombres de dominio en español** (`Egreso`, `CategoriaEgreso`, `Registrar`).
- **Pruebas automatizadas solo del dominio** (convención del repo: servicios, repos y pantallas se verifican a mano).
- **Sin migración en esta fase.** La migración y la siembra van en la fase de Infraestructura.

---

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `src/MiniERP.Domain/Finanzas/CategoriaEgreso.cs` (crear) | Catálogo de tipos de gasto: Nombre, Descripción, Activo. |
| `src/MiniERP.Domain/Finanzas/Egreso.cs` (crear) | El gasto: fecha, categoría, monto, nota, responsable + la fábrica `Registrar`. |
| `tests/MiniERP.Tests/Finanzas/EgresoTests.cs` (crear) | Las 5 pruebas de dominio. |

---

## Task 1: Entidades + `Registrar` (camino feliz)

**Files:**
- Create: `src/MiniERP.Domain/Finanzas/CategoriaEgreso.cs`
- Create: `src/MiniERP.Domain/Finanzas/Egreso.cs`
- Test: `tests/MiniERP.Tests/Finanzas/EgresoTests.cs`

**Interfaces:**
- Consumes: `MiniERP.Domain.Shared.EntidadBase` (aporta `Id`, `CreadoPor`).
- Produces: `Egreso.Registrar(decimal monto, DateTime fecha, int categoriaEgresoId, string? descripcion, string? usuarioId)` → `Egreso`. `Egreso` con `Fecha`, `CategoriaEgresoId`, `CategoriaEgreso?`, `Monto`, `Descripcion?`, `UsuarioId?`. `CategoriaEgreso` con `Nombre`, `Descripcion?`, `Activo`, `Egresos`.

- [ ] **Step 1: Write the failing tests**

Crear `tests/MiniERP.Tests/Finanzas/EgresoTests.cs`:

```csharp
using MiniERP.Domain.Finanzas;

namespace MiniERP.Tests.Finanzas;

public class EgresoTests
{
    [Fact]
    public void Registrar_valido_crea_el_egreso()
    {
        var fecha = new DateTime(2026, 7, 17);

        var egreso = Egreso.Registrar(
            monto: 5000m, fecha: fecha, categoriaEgresoId: 3,
            descripcion: "Luz de enero", usuarioId: "tester");

        Assert.Equal(5000m, egreso.Monto);
        Assert.Equal(fecha, egreso.Fecha);
        Assert.Equal(3, egreso.CategoriaEgresoId);
        Assert.Equal("Luz de enero", egreso.Descripcion);
        Assert.Equal("tester", egreso.UsuarioId);
    }

    [Fact]
    public void Registrar_sin_descripcion_deja_la_nota_nula()
    {
        var egreso = Egreso.Registrar(5000m, new DateTime(2026, 7, 17), 3, "   ", "tester");

        Assert.Null(egreso.Descripcion);
    }
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `dotnet test --filter FullyQualifiedName~EgresoTests`
Expected: FALLA de compilación — `Egreso` y `CategoriaEgreso` no existen.

- [ ] **Step 3: Write minimal implementation**

`src/MiniERP.Domain/Finanzas/CategoriaEgreso.cs`:

```csharp
using MiniERP.Domain.Shared;

namespace MiniERP.Domain.Finanzas;

/// <summary>
/// Tipo de gasto operativo: Alquiler, Luz, Agua, Sueldos, Transporte.
/// Catalogo editable; el comerciante agrega los suyos.
/// </summary>
public class CategoriaEgreso : EntidadBase
{
    public string Nombre { get; set; } = string.Empty;

    public string? Descripcion { get; set; }

    public bool Activo { get; set; } = true;

    public ICollection<Egreso> Egresos { get; set; } = [];
}
```

`src/MiniERP.Domain/Finanzas/Egreso.cs`:

```csharp
using MiniERP.Domain.Shared;

namespace MiniERP.Domain.Finanzas;

/// <summary>
/// Gasto operativo que NO pasa por el inventario: alquiler, luz, agua, sueldos, transporte.
/// No es una compra ni un pago a proveedor. Se registra ya pagado.
/// </summary>
public class Egreso : EntidadBase
{
    public DateTime Fecha { get; set; } = DateTime.UtcNow;

    public int CategoriaEgresoId { get; set; }
    public CategoriaEgreso? CategoriaEgreso { get; set; }

    public decimal Monto { get; set; }

    /// <summary>Nota opcional: "Sueldo de Maria, primera quincena".</summary>
    public string? Descripcion { get; set; }

    /// <summary>Usuario que registro el gasto (responsable). Como Pago.UsuarioId.</summary>
    public string? UsuarioId { get; set; }

    /// <summary>
    /// Registra un gasto construyendolo con sus datos. La descripcion vacia queda nula,
    /// y el usuario responsable se guarda en UsuarioId y en CreadoPor.
    /// </summary>
    public static Egreso Registrar(
        decimal monto, DateTime fecha, int categoriaEgresoId,
        string? descripcion, string? usuarioId)
    {
        return new Egreso
        {
            Monto = monto,
            Fecha = fecha,
            CategoriaEgresoId = categoriaEgresoId,
            Descripcion = string.IsNullOrWhiteSpace(descripcion) ? null : descripcion.Trim(),
            UsuarioId = usuarioId,
            CreadoPor = usuarioId
        };
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `dotnet test --filter FullyQualifiedName~EgresoTests`
Expected: PASAN (2 pruebas).

- [ ] **Step 5: Commit**

```bash
git add src/MiniERP.Domain/Finanzas/CategoriaEgreso.cs src/MiniERP.Domain/Finanzas/Egreso.cs tests/MiniERP.Tests/Finanzas/EgresoTests.cs
git commit -m "Egreso: entidades y Registrar (camino feliz)"
```

---

## Task 2: Validaciones de `Registrar`

**Files:**
- Modify: `src/MiniERP.Domain/Finanzas/Egreso.cs`
- Test: `tests/MiniERP.Tests/Finanzas/EgresoTests.cs`

**Interfaces:**
- Produces: `Egreso.Registrar` lanza `InvalidOperationException` cuando el monto es cero o negativo, o cuando falta la categoría.

- [ ] **Step 1: Write the failing tests**

Agregar a `EgresoTests.cs` (dentro de la clase, tras las pruebas de la Task 1):

```csharp
    [Fact]
    public void Registrar_con_monto_cero_falla()
    {
        var ex = Assert.Throws<InvalidOperationException>(
            () => Egreso.Registrar(0m, new DateTime(2026, 7, 17), 3, "Luz", "tester"));

        Assert.Contains("monto", ex.Message);
    }

    [Fact]
    public void Registrar_con_monto_negativo_falla()
    {
        var ex = Assert.Throws<InvalidOperationException>(
            () => Egreso.Registrar(-100m, new DateTime(2026, 7, 17), 3, "Luz", "tester"));

        Assert.Contains("monto", ex.Message);
    }

    [Fact]
    public void Registrar_sin_categoria_falla()
    {
        var ex = Assert.Throws<InvalidOperationException>(
            () => Egreso.Registrar(5000m, new DateTime(2026, 7, 17), 0, "Luz", "tester"));

        Assert.Contains("categor", ex.Message);
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `dotnet test --filter FullyQualifiedName~EgresoTests`
Expected: FALLAN las 3 nuevas — hoy `Registrar` no valida, así que no lanza ninguna excepción.

- [ ] **Step 3: Write minimal implementation**

En `Egreso.cs`, agregar las validaciones **al inicio** de `Registrar`, antes del `return`:

```csharp
    public static Egreso Registrar(
        decimal monto, DateTime fecha, int categoriaEgresoId,
        string? descripcion, string? usuarioId)
    {
        if (monto <= 0)
            throw new InvalidOperationException("El monto del egreso debe ser mayor que cero.");

        if (categoriaEgresoId == 0)
            throw new InvalidOperationException("El egreso debe tener una categoría.");

        return new Egreso
        {
            Monto = monto,
            Fecha = fecha,
            CategoriaEgresoId = categoriaEgresoId,
            Descripcion = string.IsNullOrWhiteSpace(descripcion) ? null : descripcion.Trim(),
            UsuarioId = usuarioId,
            CreadoPor = usuarioId
        };
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `dotnet test --filter FullyQualifiedName~EgresoTests`
Expected: PASAN (5 pruebas).

- [ ] **Step 5: Commit**

```bash
git add src/MiniERP.Domain/Finanzas/Egreso.cs tests/MiniERP.Tests/Finanzas/EgresoTests.cs
git commit -m "Egreso: validaciones de Registrar (monto positivo, categoria requerida)"
```

---

## Cierre de la fase

- [ ] **Correr toda la suite** para confirmar que no se rompió nada:

Run: `dotnet test`
Expected: 118 pruebas verdes (113 previas + 5 nuevas).

---

## Fases siguientes (planes aparte, sin bloqueo)

1. **Aplicación:** `IEgresoService` (buscar por rango + categoría con total, obtener, guardar vía `Registrar`, eliminar) y `ICategoriaEgresoService` (buscar, opciones, guardar, activar/desactivar), con sus contratos de repositorio y DTOs.
2. **Infraestructura + migración:** configuración EF de las dos entidades, `DbSet`, repositorios, **siembra del starter** (Alquiler, Luz, Agua, Sueldos, Transporte) en `DatabaseInitializer`, y la migración `AgregarFinanzasEgreso` (traer `main` antes).
3. **Web:** pantallas de registrar egreso, lista con filtro/total/"registrado por", y administración de categorías.
4. **Capítulo IV:** la sección del egreso, con capturas cuando exista la pantalla.
